# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - ubc-fashion-studio-ai-backend

on:
  push:
    branches:
      - feature/main-backend-deployment
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://ghcr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_471cceedd7fa4c44955901bad9519b38 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_9bbf5c01358043038f88a791959a6c93 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: ghcr.io/${{ secrets.AzureAppService_ContainerUsername_471cceedd7fa4c44955901bad9519b38 }}/kanakamsasikalyan/fashion-studio-ai:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'ubc-fashion-studio-ai-backend'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_8542be066ca04aa385b99143408e2c89 }}
        images: 'ghcr.io/${{ secrets.AzureAppService_ContainerUsername_471cceedd7fa4c44955901bad9519b38 }}/kanakamsasikalyan/fashion-studio-ai:${{ github.sha }}'